export enum AllowedPlatforms {
  YOUTUBE = "youtube",
  NETFLIX = "netflix",
  SPOTIFY = "spotify",
  CLAUDE = "claude",
  DISNEY = "disney",
}

export const allowedPlatformsArray = Object.values(AllowedPlatforms);
